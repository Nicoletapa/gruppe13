@model EditRoleViewModel

@{
    ViewBag.Title = "Edit Role";
}

<div class="centered-container">
    <div class="form-container">
        <h1 class="text-center">Rediger rolle</h1>

        <form method="post" class="mt-3">
             @Html.AntiForgeryToken()
            <div class="form-group">
                <label asp-for="Id" class="col-form-label">RolleID</label>
                <input asp-for="Id" disabled class="form-control">
            </div>
            <div class="form-group">
                <label asp-for="RoleName" class="col-form-label">Rollenavn</label>
                <input asp-for="RoleName" class="form-control">
                <span asp-validation-for="RoleName" class="text-danger"></span>
            </div>

            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group button-group">
                <a asp-action="ListRoles" class="btn btn-secondary">Avbryt</a>
                <button type="submit" class="btn btn-primary">Oppdater</button>
            </div>
        </form>
    </div>
</div>
